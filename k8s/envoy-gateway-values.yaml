# Envoy Gateway controller chart values.
# A single replica is a single point of failure for all ingress; raise it
# and add a PodDisruptionBudget where availability matters.
deployment:
  replicas: 1
service:
  type: ClusterIP
